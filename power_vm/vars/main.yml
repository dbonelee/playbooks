---
# vars file for power_vm
vm_name: "{{ lookup('env','vm_name') }}"
vcenter_host: "{{ lookup('env','vcenter_host') }}"
username_secret: "{{ lookup('env','username_secret') }}"
password_secret: "{{ lookup('env','password_secret') }}"
datacenter_name: "{{ lookup('env','datacenter_name') }}"
folder_name: "{{ lookup('env','folder_name') }}"
datacenter_name: "{{ lookup('env','datacenter_name') }}"
cluster_name: "{{ lookup('env','cluster_name') }}"
template_name: "{{ lookup('env','template_name') }}"
network_label: "{{ lookup('env','network_label') }}"
ip_address: "{{ lookup('env','ip_address') }}"
netmask: "{{ lookup('env','netmask') }}"
gateway: "{{ lookup('env','gateway') }}"
dns_1: "{{ lookup('env','dns_1') }}"
dns_2: "{{ lookup('env','dns_2') }}"
datastore: "{{ lookup('env','datastore')}}"
guest_secret: "{{ lookup('env','guest_secret') }}"
power: "{{ lookup('env','power') }}"